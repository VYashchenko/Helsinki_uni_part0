``` mermaid
    sequenceDiagram
        participant client
        participant server

        client ->> server: GET https://studies.cs.helsinki.fi/exampleapp/spa
        activate server
        server -->> client: the HTML file
        deactivate server

        client ->> server: GET https://studies.cs.helsinki.fi/exampleapp/main.css
        activate server
        server -->> client: the CSS file
        deactivate server

        client ->> server: GET https://studies.cs.helsinki.fi/exampleapp/spa.js
        activate server
        server -->> client: the JS file
        deactivate server

        Note right of client: The browser starts executing the JavaScript code that fetches the JSON from the server

        client ->> server: GET https://studies.cs.helsinki.fi/exampleapp/data.json
        activate server
        server -->> client:  [{ "content": "HTML is easy", "date": "2023-1-1" }, ... ]
        deactivate server

        Note right of client: All the next requests from the client will only update the partial part of the web page.(It will load the client)


```
